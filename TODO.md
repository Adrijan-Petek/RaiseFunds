# TODO

- Add `NEXT_PUBLIC_MAINNET_RPC_URL` (and `NEXT_PUBLIC_BASE_RPC_URL` if needed) to `.env.local` for reliable name resolution.
- Verify Basenames resolve for wallets without ENS (fallback should remain silent on errors).
- Confirm wallet icons render correctly in the dropdown on mobile and desktop.
- Review dropdown spacing/width for long wallet labels.
